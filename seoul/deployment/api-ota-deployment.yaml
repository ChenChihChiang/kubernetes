apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: api-ota-deployment
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 2
  minReadySeconds: 30
  template:
    metadata:
      labels:
        app: api-ota-pod
    spec:
      containers:
      - name: api-ota-container
        image: nuwarobotics/api-ota:0.1.6-SNAPSHOT
        imagePullPolicy: Always
        ports:
        - name: api-ota-port
          containerPort: 7100
        env:
        - name: EXEC_ENV
          value: dev
        livenessProbe:
          tcpSocket:
            port: 7100
          initialDelaySeconds: 15
          timeoutSeconds: 30
      imagePullSecrets:
      - name: regsecret

