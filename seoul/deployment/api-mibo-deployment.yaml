apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: api-mibo-deployment
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 2
  minReadySeconds: 30
  template:
    metadata:
      labels:
        app: api-mibo-pod
    spec:
      containers:
      - name: api-auth-container
        image: nuwarobotics/api-mibo:0.1.12-SNAPSHOT
        imagePullPolicy: Always
        ports:
        - name: api-ota-port
          containerPort: 8100
        env:
        - name: EXEC_ENV
          value: dev
        livenessProbe:
          tcpSocket:
            port: 8100
          initialDelaySeconds: 15
          timeoutSeconds: 30
      imagePullSecrets:
      - name: regsecret

